{
  "name": "fitpilot",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c yellow,cyan \"npm run dev --prefix fitpilot.backend\" \"npm run dev --prefix fitpilot.frontend\"",
    "dev:backend": "npm run dev --prefix fitpilot.backend",
    "dev:frontend": "npm run dev --prefix fitpilot.frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
